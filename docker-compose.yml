version: '3'
services:
  shadowsocks:
    
    ports:
      - 9229:9229
     
 
    build:
      context: .
        
      dockerfile: Dockerfile
    environment:
      - METHOD=rc4 # 加密方法
      - PASSWORD=170826  # 密码
   
    restart: always