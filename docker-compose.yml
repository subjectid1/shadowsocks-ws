version: '3'
services:
  shadowsocks:
    
    ports:
      - 443:443
     
 
    build:
      context: .
        
      dockerfile: Dockerfile
    environment:
      - METHOD=chacha20-ietf-poly1305 # 加密方法
      - PASSWORD=170826  # 密码
   
    restart: always